export function premiumGate(account, backHref = "#/mock") {
  return `<main class="v26-page"><a class="v26-back" href="${backHref}">← Mock Exam</a><section class="v26-route-gate"><p class="v26-kicker">Premium Practice</p><h1>${account ? "Full exam simulation is included with Premium." : "Sign in to take a mock."}</h1><p>${account ? "Quick and Full mocks, persisted sittings, history, question review, and detailed exam analytics require an active Premium membership." : "Candidate authentication and an active Premium membership are required for persisted mock exams."}</p>${account ? `<a class="v26-btn primary" href="#/membership">View Premium</a>` : `<button class="v26-btn primary" type="button" data-auth-intent="signup">Create Free Account</button><button class="v26-btn secondary" type="button" data-auth-intent="login">Sign In</button>`}</section></main>`;
}
