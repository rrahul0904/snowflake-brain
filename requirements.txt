# Keep this in sync with pyproject.toml. FastAPI owns the compatible
# Starlette constraint, so do not pin an unrelated Starlette release here.
# 0.137.2 is the first release line used here that supports patched Starlette 1.x.
fastapi==0.137.2
uvicorn[standard]==0.32.1
pydantic==2.10.3
httpx==0.28.1
google-auth>=2.38,<3
requests>=2.32,<3
psycopg[binary]>=3.2,<4
psycopg-pool>=3.2,<4
